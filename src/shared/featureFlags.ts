/**
 * Public Early Preview feature gates.
 *
 * Keep unfinished modules in the codebase without exposing incomplete product
 * promises in release builds. Flip only after the full workflow is tested.
 */
export const ENABLE_INSPIRATION_BOARD = false;
