# Embedding model

Bundled INT8 ONNX weights for local semantic search:

- `model_int8.onnx` (in this repository)
- tokenizer / config JSON alongside it

See [MODEL_LICENSE.md](./MODEL_LICENSE.md).

If you need to re-download the weights:

```bash
./scripts/fetch-embedding-model.sh
```
